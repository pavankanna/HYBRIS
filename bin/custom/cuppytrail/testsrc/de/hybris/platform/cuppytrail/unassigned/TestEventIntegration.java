package de.hybris.platform.cuppytrail.unassigned;

import de.hybris.platform.servicelayer.ServicelayerTransactionalTest;


/**
 * The purpose of this test is to illustrate the EventService and how events can be sent and recieved during key moment
 * in a Model's lifecycle
 * 
 * @see "https://wiki.hybris.com/display/pm/Trail+~+????????????"
 */
public class TestEventIntegration extends ServicelayerTransactionalTest
{
	/*
	 * @Resource private IStadiumService stadiumService;
	 * 
	 * @Resource private ModelService modelService;
	 * 
	 * @Test public void testMyProductModelInterceptor() {
	 * 
	 * final CatalogModel cat = new CatalogModel(); cat.setId("cat"); final CatalogVersionModel catalogVersion = new
	 * CatalogVersionModel(); catalogVersion.setCatalog(cat); catalogVersion.setVersion("online");
	 * 
	 * 
	 * final StadiumModel stadiumModel = stadiumService.getAllStadium().get(0); stadiumModel.setCode("");
	 * stadiumService.doSomethingThatTriggersAnEvent(stadiumModel); assertEquals(MyEventListener.LISTENER_CAUGHT_EVENT,
	 * stadiumModel.getCode()); }
	 */
}
