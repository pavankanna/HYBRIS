package de.hybris.platform.cuppytrail.unassigned;



/**
 * This test relates to the Tutorial Trail Step found at https://wiki.hybris.com/display/pm/Trail+~+New+Data+Model
 * 
 */
public class StadiumServiceStrategyTest
{
	/*
	 * private IStadiumService stadiumService; private IStadiumDAO stadiumDAO; private IStadiumStrategy stadiumStrategy;
	 * 
	 * @Before public void setUp() { stadiumService = new StadiumServiceImpl(); stadiumDAO =
	 * createMock(IStadiumDAO.class); stadiumService.setStadiumDAO(stadiumDAO);
	 * 
	 * stadiumStrategy = createMock(IStadiumStrategy.class); stadiumService.setStadiumStrategy(stadiumStrategy); }
	 * 
	 * 
	 * @Test public void testCallingStrategy() { final StadiumModel stadiumModel = new StadiumModel();
	 * stadiumModel.setLocaleProvider(new StubLocaleProvider(Locale.GERMAN)); stadiumModel.setCode("codeStadium1");
	 * 
	 * expect(stadiumStrategy.somethingComplex()).andReturn(stadiumModel); replay(stadiumStrategy);
	 * stadiumService.callStrategy(); verify(stadiumStrategy); }
	 */
}
