/**
 * 
 */
package de.hybris.platform.cuppytrail.unassigned;

import de.hybris.platform.servicelayer.ServicelayerTransactionalTest;


/**
 * @author ken.lomax
 * 
 */
public class TestStrategy extends ServicelayerTransactionalTest
{
	/*
	 * // If this extends ServicelayerTransactionalTest then we can inject it via @Resource // @Resource private
	 * IStadiumStrategy stadiumStrategy;
	 * 
	 * // .. or we create via a constructor
	 * 
	 * @Before public void setUp() { stadiumStrategy = new StadiumStrategy(); }
	 * 
	 * @Test public void testGetMyProductsStrategy() { final StadiumModel result = stadiumStrategy.somethingComplex();
	 * assertEquals("Strategy should return 42", "codeMyProduct1", result.getCode()); }
	 */
}
